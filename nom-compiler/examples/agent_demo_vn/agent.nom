cai khai_niem minimal_safe_agent muc_dich compose a small set of tools an llm can plan with safely.

dung cai khai_niem agent_safety_policy,
     cai ham read_file khop "read text from a workspace path",
     cai ham write_file khop "write text to a workspace path",
     cai ham list_dir khop "list files in a workspace directory",
     cai ham fetch_url khop "fetch the body of an https url",
     cai ham search_web khop "search the web and return result links",
     cai ham run_command khop "run an allowed shell command".

bay_ra read_file, write_file, list_dir, fetch_url, search_web, run_command.

khi the safety policy is composed.
khi every exposed tool has at least one require clause.

uu_tien security roi composability roi speed.
