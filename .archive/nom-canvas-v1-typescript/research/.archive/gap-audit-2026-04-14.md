# Gap Audit — 2026-04-14

## Current Code Truth

- Repo head verified: `98bef38`
- GitNexus refresh completed against current `HEAD`
- GitNexus analyzer result: `5,856 nodes`, `14,682 edges`, `267 clusters`, `300 flows`
- Cargo workspace members: `31`
- `cargo check --workspace --message-format short`: passes
- Targeted crate tests pass:
  - `nom-concept`
  - `nom-lsp`
  - `nom-grammar`
  - `nom-intent`
  - `nom-graph`

Important live surfaces confirmed in code:

- `nom-grammar` exists, has CLI wiring for `nom grammar init`, `nom grammar import`, and `nom grammar status`, and `cargo test -p nom-grammar --quiet` passes
- `nom-lsp` exists and tests pass
- `nom-intent` exists and tests pass
- `nom-graph` is a live CLI surface through `cmd_graph`
- `nom-translate` is no longer name-only scaffolding; `translate()` dispatches to C/C++/Python/JS/Go and is wired to `cmd_translate`

## Archive Verdicts

### Current and valuable

These are still strong active references:

- `research/.archive/language-analysis/10-external-repo-upgrade-plan.md`
- `research/.archive/language-analysis/13-nomx-strictness-plan.md`
- `research/.archive/language-analysis/20-session-summary-2026-04-14.md`
- `research/.archive/language-analysis/22-dict-split-migration-plan.md`

Why they remain valuable:

- they already acknowledge newer crates and newer wedge work
- they are closer to the current repo frontier than the older top-level mission docs
- they still map cleanly onto live code surfaces like `nom-grammar`, `nom-lsp`, `nom-intent`, `nom-graph`, and the `nom-concept` strictness pipeline

### Partially stale but valuable

These docs still matter, but their status banners or shipped/planned summaries are no longer fully accurate:

- `research/.archive/deferred/06-blueprint-for-building-novel.md`
- `research/.archive/language-analysis/04-next-phases-plan.md`
- `research/.archive/language-analysis/21-grammar-registry-design.md`
- `research/.archive/language-analysis/02-fifty-language-analysis.md`
- `research/.archive/motivation/01-world-language-survey.md`
- `research/.archive/motivation/02-vietnamese-grammar-to-novel-syntax.md`
- `research/.archive/language-analysis/09-implementation-status-2026-04-13.md`

### Historical snapshots

These are still useful for provenance, but should not be treated as current-state summaries:

- `research/.archive/session-notes/2026-04-13-autonomous-sdd-arc.md`
- `research/.archive/language-analysis/03-self-hosting-roadmap.md`
- older motivation/deferred thesis docs that primarily capture rationale rather than verified implementation state

### Superseded mission themes

These should not remain on an active mission list:

- adopting Vietnamese tokens into shipped Nom source
- using locale packs as source-keyword translation layers
- any roadmap assumption that `nom-lsp`, `nom-grammar`, `nom-intent`, or `nom-graph` do not yet exist

## Most Important Stale Claims

### 1. Blueprint doc overstates shipped scope

File:

- `research/.archive/deferred/06-blueprint-for-building-novel.md`

Stale claim:

- says phases `0-7` and `9` have shipped and cites `28` crates

Current reality:

- workspace is now `31` crates
- the build is green again, but the doc is still not a trustworthy current-state summary

Verdict:

- partially stale but still valuable as a blueprint

### 2. Next-phases plan still says `nom-lsp` does not exist

File:

- `research/.archive/language-analysis/04-next-phases-plan.md`

Stale claim:

- phase 9 described as `0% code started`
- `nom-lsp` stated as nonexistent

Current reality:

- `nom-lsp` exists
- GitNexus indexes it
- `cargo test -p nom-lsp --quiet` passes

Verdict:

- stale on LSP status, still valuable as a roadmap

### 3. Grammar-registry design doc still calls itself draft-only

File:

- `research/.archive/language-analysis/21-grammar-registry-design.md`

Stale claim:

- says it still requires the implementation wedge

Current reality:

- `nom-grammar` exists
- grammar CLI is wired
- `cargo test -p nom-grammar --quiet` passes
- `cargo test -p nom-grammar -- --list` shows baseline import and audit tests

Verdict:

- stale status banner, still valuable design reference

### 4. Older “last verified” banners are now out of date

Files:

- `research/.archive/motivation/01-world-language-survey.md`
- `research/.archive/motivation/02-vietnamese-grammar-to-novel-syntax.md`
- `research/.archive/language-analysis/04-next-phases-plan.md`

Stale claim:

- still cite older verified heads such as `afc6228`

Current reality:

- this audit used `98bef38`

Verdict:

- these need re-verification banners before being treated as current-state truth

### 5. `nom-translate` is understated in older docs

File:

- `research/.archive/language-analysis/02-fifty-language-analysis.md`

Stale claim:

- still frames `nom-translate` mostly as scaffold work

Current reality:

- live `translate()` dispatch exists for C/C++/Python/JS/Go
- CLI integration through `cmd_translate` is present

Verdict:

- partially stale / understated

## Confirmed Pending Mission Areas

These are still genuinely open in the codebase and docs:

- corpus pilot / meaningful ingestion at scale
- embedding/rerank resolver beyond the current stub path
- full contract enforcement at flow edges
- planner-in-Nom
- parser-in-Nom
- fixpoint bootstrap
- fuller LSP / Authoring Protocol expansion
- dict-split follow-through and naming cleanup

## Tooling Gap

GitNexus is authoritative for live code truth in this repo, but it does not currently index `research/.archive/**` because the directory is hidden.

That means archive verification currently requires a mixed workflow:

- GitNexus for code truth
- local markdown audit for archive truth

This is the main reason archive mission docs were able to drift without an automatic re-verification loop.
