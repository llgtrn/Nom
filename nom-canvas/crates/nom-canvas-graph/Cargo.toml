[package]
name = "nom-canvas-graph"
version = "0.1.0"
edition = "2021"

[dependencies]
nom-blocks = { path = "../nom-blocks" }
nom-memoize = { path = "../nom-memoize" }
lru = { workspace = true }

[lints]
workspace = true
